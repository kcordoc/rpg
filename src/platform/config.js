/**
 * QuestForge Platform Configuration
 *
 * Platform-level constants that are the same regardless of which game is loaded.
 * Game-specific values live in each game's config (see ./games/).
 */

export const PLATFORM_NAME = 'QuestForge';
export const PLATFORM_COMPANY = 'CORDOC LLC';
export const PLATFORM_URL = 'https://questforge.io';

// The default game slug to load when no ?game= param is present
export const DEFAULT_GAME_SLUG = 'lipid-wizard';
